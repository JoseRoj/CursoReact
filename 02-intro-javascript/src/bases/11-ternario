const activo = true;
let mensaje = '';
if (activo) {
  mensaje = 'Activo';
}else {
  mensaje = 'Inactivo';
}
console.log(mensaje);

const men = activo && 'Activo'
mensaje = (activo) ? 'Activo' : 'Inactivo';