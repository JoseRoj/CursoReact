describe("Pruebas de demostración", () => {
  test("Esta es una prueba de demostración", () => {
    const message = "Hello World";
    const message2 = message.trim();
    expect(message).toBe(message2);
  });
});
